// components
import { Cart } from "../components";

// types
import type { JSX } from "preact/jsx-runtime";

export const CartPage = (): JSX.Element => {
  return (
    <>
      <Cart />
    </>
  );
};
